# DOLCE

A multiplayer browser social-deduction game.

## Run

```bash
npm install
npm start
```

Open `http://localhost:3000` and host. Other players join from their phones on the same network using the laptop's IP (e.g. `http://192.168.x.x:3000`).
